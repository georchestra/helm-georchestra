---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: georchestra-extractorapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: georchestra-extractorapp
  template:
    metadata:
      labels:
        app: georchestra-extractorapp
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      initContainers: []
      containers:
      - name: georchestra-extractorapp
        image: georchestra/extractorapp:latest
        volumeMounts:
          - mountPath: /etc/georchestra
            name: georchestra-datadir
        ports:
        - containerPort: 8080
          name: http-proxy
      volumes:
        {{- .Values.georchestra.datadir.volume | toYaml | nindent 6 }}