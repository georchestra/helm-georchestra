apiVersion: v1
kind: Service
metadata:
  name: {{ include "georchestra.fullname" . }}
  labels:
    {{- include "georchestra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "georchestra.selectorLabels" . | nindent 4 }}
