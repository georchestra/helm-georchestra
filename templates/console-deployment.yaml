---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: georchestra-console
spec:
  replicas: 1
  selector:
    matchLabels:
      app: georchestra-console
  template:
    metadata:
      labels:
        app: georchestra-console
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      initContainers: []
      containers:
      - name: georchestra-console
        image: georchestra/console:latest
        volumeMounts:
          - mountPath: /etc/georchestra
            name: georchestra-datadir
        ports:
        - containerPort: 8080
          name: http-proxy
      volumes:
        {{- .Values.georchestra.datadir.volume | toYaml | nindent 6 }}
